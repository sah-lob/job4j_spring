<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                 http://www.springframework.org/schema/beans/spring-beans.xsd
                 http://www.springframework.org/schema/security
                 http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true">
        <intercept-url pattern="/login*" access="permitAll()"/>
        <intercept-url pattern="/resource/css/login*" access="permitAll()"/>
        <intercept-url pattern="/**" access="hasRole('ROLE_ADMIN')"/>
        <form-login login-page="/login.do"
                    default-target-url="/index.do"
                    authentication-failure-url="/login.do?error=error"
                    username-parameter="login2"
                    password-parameter="password3"
                    login-processing-url="/login.do"
        />
        <logout logout-url="/logout" logout-success-url="/index"/>
        <csrf disabled="true"/>
    </http>

    <beans:bean id="encoder"
                class="org.springframework.security.crypto.password.NoOpPasswordEncoder">
    </beans:bean>

    <authentication-manager>
        <authentication-provider>
            <password-encoder ref="encoder"/>
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query=
                                       "SELECT login, password, active FROM persons WHERE login = ?"
                               authorities-by-username-query="SELECT login, role FROM persons WHERE login = ?"
            />
        </authentication-provider>
    </authentication-manager>

</beans:beans>

